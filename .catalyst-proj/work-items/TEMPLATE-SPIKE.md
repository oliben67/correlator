# `SPIKE-NNNN` — short title

Time-boxed research/investigation, not a commitment to build anything.

| Field | Value |
|---|---|
| **ID** | `SPIKE-NNNN` |
| **Status** | open / answered / abandoned |
| **Parent** | `STORY-NNNN` or `EPIC-NNNN` this unblocks |
| **Timebox** | e.g. "1 day" — hard stop, not an estimate |
| **Related rule(s)** | existing rule ID(s) concerned, if any |
| **Signed-off-by** | name of the registered user (`development/users.json`) who signed this spike — see `CODE-OF-CONDUCT.md` §2 |

## Question

Phrased so "answered: yes/no/X" is unambiguous.

## Findings

Filled in at timebox end — enough detail to estimate the parent from this
alone.

## Outcome

- New rule(s) needed → hand off to a `REQ-NNNN`'s New rules proposed.
- No new rule, just an approach → note in the parent story directly.
- Unresolved within timebox → re-scope as a smaller follow-up spike.

## Related

Parent `STORY-`/`EPIC-` ID, and any `REQ-`/`BUG-` doc this produced.
