# Backlog

> Maintained by `/show-backlog` — this file is regenerated in full each
> time that command runs, not hand-edited. See `CODE-OF-CONDUCT.md` §3 and
> the catalyst framework's `INVARIANTS.md` INV-14.

**Last refreshed:** 2026-08-23 by `/show-backlog`.

## Open bugs

*(none)*

## In-progress / proposed requirements

- `REQ-0001-initial-telemetry-log-correlation` — proposed, targets
  `cor-CORE-001-telemetry-log-correlation`.

## Work items missing links

*(none)* — no work items (`EPIC-`/`STORY-`/`TASK-`/`SPIKE-`/`SPRINT-`)
exist yet.

## Rules with no open work

*(none)* — `cor-CORE-001` (❌ not implemented) is targeted by `REQ-0001`;
every `env-*` rule is ✅ working.

## Feature ideas with no requirement yet

- `FEAT-0001-cttc-port-electron-react-python-sump` — planned, no
  `Requirement(s)` yet (expected: the roadmap's phases each open their own
  `REQ-NNNN` when that phase starts, per
  `docs/roadmap/cttc-to-correlator-port.md` §13 traceability).

## Roadmap

*(none)* — no named roadmap has been added yet (`/roadmap-add`).
